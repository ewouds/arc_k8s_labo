apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: mesh-demo
  labels:
    app: backend
    environment: workshop
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      name: http
  selector:
    app: backend
    # No version selector — routes to both v1 and v2
